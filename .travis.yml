language: node_js
node_js:
    - "10"

after_script:
    - gitbook build
    - cd ./_book
    - git init
    - git config user.name "gaotianjun"
    - git config user.email "214938630@qq.com"
    - git add .
    - git commit -m "publish gitbook"
    - git push --force --quiet "https://9fbb63ecd656edd13e79a459e8eca9d9daae1b60@https://gaotianjun.github.io/ibooks/" master:ph-pages

branches:
    only:
          - master
